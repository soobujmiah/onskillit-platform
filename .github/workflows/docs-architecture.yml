name: Documentation architecture
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  registers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check canonical phase and page registers
        run: python3 scripts/check_docs_architecture.py
