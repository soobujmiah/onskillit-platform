# V1 payment architecture and provider evaluation

Status: **V1 real-payment workflow confirmed; bKash, Nagad and SSLCOMMERZ requested; merchant access and commercial/legal terms open**. This document is architecture research, not permission to open merchant accounts, move money or enable production checkout.

## Required real-life workflows

Paid course enrollment and service invoices must create a server-owned order/invoice with fixed line items, BDT minor-unit amount, tax/discount policy and expiration. Checkout creates a `PaymentIntent` with a unique idempotency key and provider reference. Customer redirect/success page is only UX feedback; the server confirms payment through provider notification and independent validation/query. The invoice/enrollment becomes paid/admitted only after matching provider status, amount, currency and merchant account. Pending and failed states remain visible with retry guidance. Duplicate or out-of-order callbacks never duplicate admission or money entries. A reconciliation job compares provider settlement/transaction reports to local ledger and raises exceptions for staff review.

Refund: authorized staff request with reason and policy check → optional second approval above threshold → provider request → pending/refunded/failed status confirmation → compensating ledger entry and enrollment/service impact. Partial payments, partial refunds, chargebacks and manual adjustments require explicit policy. Offline payment remains a controlled route: record method/reference/evidence, assign staff, second-person approval when appropriate, reconcile to bank/MFS statement. Do not collect/store card PAN, PIN or OTP. Financial reports separate gross, fees, refunds and net receipts.

## Provider abstraction

`PaymentProvider` contract: create checkout, validate notification, query transaction, request/query refund, normalize provider errors, report capabilities. Provider-specific IDs/raw payloads stay in a restricted adapter table; core invoice/enrollment state machine uses normalized events. Adapter is versioned and sandbox-tested. Callbacks/IPN have provider-specific authentication and independent validation, replay protection, retry-safe response, minimal logs and no secret exposure. Payment amounts are immutable after intent creation; a changed invoice requires a new intent. Currency is explicit. Merchant credentials live in protected deployment secrets, never GitHub source or CI logs.

## Candidate review (official sources, accessed 2026-09-24)

| Candidate | Evidence | Decision requirement |
|---|---|---|
| SSLCOMMERZ hosted checkout | [Official integration docs](https://developer.sslcommerz.com/doc/v4/) describe session creation, IPN, required order validation and refund API; [sandbox docs](https://sandbox-gw.sslcommerz.com/docs) describe test environment | Confirm current merchant terms, supported methods/fees, contract, IPN/network requirements and API currency/version with provider |
| bKash online business/PGW | [Official business page](https://www.bkash.com/en/business) lists gateway and refunds; [merchant page](https://www.bkash.com/en/business/merchant) describes merchant account | Confirm merchant eligibility, API access, current docs, settlement, fees and refunds |
| Nagad merchant checkout | Requested by founding partner; public official integration documentation was not located in this research | Obtain official Nagad merchant documentation/sandbox/contract directly; verify API and refund/reconciliation capabilities before adapter design |

**Confirmed target coverage:** bKash, Nagad and SSLCOMMERZ, with room for other approved providers. These may be three direct adapters, or a smaller number of contracts if one gateway demonstrably supplies a method under acceptable commercial terms; the UI must show the actual processor and avoid duplicate/confusing choices. **Provisional evaluation order:** confirm SSLCOMMERZ method coverage and terms, direct bKash merchant access, and official Nagad integration access. This is an inference from official product descriptions and the partner's preference, not a contract or final integration choice. Merchant fees, settlement and legal eligibility are volatile and require direct current quotes. Never use personal-wallet transaction scraping or an unofficial relay as a substitute for an authorized merchant integration. Additional options need the same licensing, security, reconciliation and support review.

## Acceptance and release gate

In GitHub CI with sandbox fixtures: success, fail, cancel, pending, delayed callback, duplicate callback, mismatched amount/currency, unknown intent, refund request/status, reconciliation discrepancy and cross-account denial. In staging: provider sandbox end-to-end and manual/offline approval flow. Production requires merchant contract and credentials, verified registered legal-entity name, legal/tax/refund approval, customer-visible terms, finance-role review, backup/incident plan and owner approval. A payment provider sandbox pass is not proof that live settlement works.

## Provider readiness matrix — official-source review, 2026-09-24

These are public documentation facts, not OnSkillIT merchant capabilities. Provider documentation and terms may change; recheck at integration. The adapter contract above remains the only core-commerce dependency.

| Topic | SSLCOMMERZ | bKash | Nagad |
|---|---|---|---|
| Official public API/sandbox | **VERIFIED:** official [v4 docs](https://developer.sslcommerz.com/doc/v4/) describe sandbox registration, session creation, hosted/easy checkout, IPN, order validation and separate live/test store IDs. Version page says updated 2019, so current contract must be reconfirmed. | **VERIFIED product claim:** official [business page](https://www.bkash.com/en/business) offers payment gateway, APIs and instant refunds. Public merchant-specific API schema, credentials, sandbox and callback contract were **not verified** from official public docs. | **VERIFIED merchant-payment category only:** official [Nagad service page](https://nagad.com.bd/services/?service=merchant-payment). Official public merchant API/sandbox contract was **not located**; do not rely on third-party plugin examples. |
| Merchant onboarding | Official sandbox and production registration links in v4 docs; actual OnSkillIT store approval/credentials **UNKNOWN**. | Official [merchant](https://www.bkash.com/en/business/merchant) and [online-business](https://www.bkash.com/en/business/online-business) forms ask for business/contact and trade-license/bank/NID information; eligibility and issued API access **UNKNOWN**. | Merchant enrollment, required legal documents and access **UNKNOWN**; obtain directly from Nagad or an approved licensed aggregator. |
| Initiation/authentication | Store ID/password and server-side session request documented; keys must be secret. | Product-level API availability verified, exact auth/initiation contract **UNKNOWN** until merchant package. | **UNKNOWN** until official merchant package. |
| Callback/verification/status | Official docs require reachable POST IPN then independent Order Validation API; returned `VALID`, `FAILED`, `CANCELLED` and `risk_level` handling are documented. Customer redirect is not settlement proof. | Callback, query and status semantics **UNKNOWN** from verified official public material. | Callback, query and status semantics **UNKNOWN**. |
| Refund/reconciliation/settlement | Official [developer docs](https://developer.sslcommerz.com/docs.html) document refund request/status APIs; actual merchant enablement, settlement exports, current fees and timing **UNKNOWN**. | Official business page claims instant refunds and transaction history; exact API, settlement and reconciliation files **UNKNOWN**. | Refund/query/settlement interface **UNKNOWN**. |
| Network/security | Official docs describe TLS 1.2+, internet-reachable IPN and live public-IP registration; exact allowlist values must be reconfirmed with provider, not hard-coded from old docs. | Merchant security, signature/token lifetime, webhook authentication and allowlists **UNKNOWN** until official integration pack. | All provider-specific controls **UNKNOWN**. |
| OnSkillIT decision | **EXTERNAL DEPENDENCY:** approved live/sandbox merchant account, current integration version, callback reachability, IP registration, fees and refund terms. | **EXTERNAL DEPENDENCY:** contract, API package/sandbox, credentials and operational terms. | **EXTERNAL DEPENDENCY:** official contract/docs/sandbox or confirmation of coverage through an approved gateway. |

**Cross-provider acceptance contract:** create with immutable invoice amount/currency; pending/expired/fail/cancel/success; late, repeated and out-of-order callback; independent status verification; risk hold; idempotent ledger/admission; refund pending/failed/complete; settlement discrepancy; outage/retry; secret rotation; account isolation. All provider-specific tests run in GitHub with sanctioned sandbox or synthetic contract fixtures. No real checkout is enabled without legal issuer, customer terms, approved merchant access and PHASE-14 production gate. Never put example provider credentials from documentation into repository config or logs.

## Accepted adapter and state contract — ADR 0008

`PaymentProvider` must expose `capabilities`, `createPayment`, `verifyPayment`, `getStatus`, `handleCallback`, `handleWebhook`, `refund` and `reconcile` as typed operations. Adapter-specific unsupported functions return an explicit capability error and cannot silently simulate success. `PaymentIntent` owns immutable amount/currency/invoice; `PaymentAttempt` owns one provider checkout reference and lifecycle; `ProviderEvent` retains a restricted event digest and deduplication key; `PaymentTransaction`, `Refund` and `SettlementEntry` are append-only financial history. Core statuses are `created → pending → paid | failed | cancelled | expired`, with `review` for unmatched/risk status. Refund has `requested → pending → succeeded | failed`, independent of paid transaction history. Network timeout leaves status pending and schedules verification; it never assumes failure or success. A duplicate callback returns the prior accepted result. Paid/admitted transition is one database transaction with an outbox event. Reconciliation compares provider status/settlement against local immutable entries and routes exceptions to a finance queue. Provider credentials are environment secrets, not CMS fields.

Merchant account, official provider pack and sandbox/live credentials remain external dependencies. SSLCOMMERZ official API is publicly documented; bKash and Nagad details must be implemented only from provider-issued current contracts. The core architecture is approved now; no live provider capability is claimed.
